<Window x:Class="TimeManagement.messageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TimeManagement"
        mc:Ignorable="d"
        Title="" Icon="/Icons/To-Do_icon.png" Height="200" Width="400" ResizeMode="CanMinimize" Style="{StaticResource ResourceKey=windowStyle}">
    <Grid>
        <TextBlock x:Name="message" Text="" HorizontalAlignment="Stretch" VerticalAlignment="Top" MaxHeight="100" Margin="30 25 30 0" FontWeight="Normal" TextWrapping="Wrap" Style="{StaticResource ResourceKey=textBlockStyle}"/>
        <Button x:Name="buttonYes" Content="Yes" VerticalAlignment="Bottom" HorizontalAlignment="Left" Height="40" Width="120" Margin="50 0 50 25" Style="{StaticResource ResourceKey=buttonStyle}" Click="buttonYes_Click"/>
        <Button x:Name="buttonNo" Content="No" VerticalAlignment="Bottom" HorizontalAlignment="Right" Height="40" Width="120" Margin="50 0 50 25" Style="{StaticResource ResourceKey=buttonStyle}" Click="buttonNo_Click"/>
    </Grid>
</Window>
